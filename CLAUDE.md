# CLAUDE.md

Read [AGENTS.md](AGENTS.md) first — it is the canonical conventions doc
(commands, architecture, sharp edges, shipping rules).

Claude-specific notes:

- `pnpm-lock.yaml` is prettier-formatted; run `pnpm run format` after
  dependency changes.
